import { YellowBox } from 'react-native';

YellowBox.ignoreWarnings([
  'Warning: Cannot update during an existing state transition',
]);

var ViroSample = require('./js/Frame');

module.exports = ViroSample;
